# AI Shopping Guide Agent · 面试口述稿

> 配合本地 Demo 使用：`streamlit run app.py`  
> 建议提前打开 Top 3 推荐、Score Breakdown、AB 看板页面，口述时结合界面演示。

---

## 30 秒中文介绍

我做了一个 **AI 电商导购 Agent** 的可运行 Demo。用户用自然语言说需求，系统会解析预算区间、品类和偏好，从商品库里召回候选，再用多目标排序给出 Top 3，并解释为什么推荐、为什么不推荐别的商品。同时设计了埋点和 AB 实验框架。这个项目对应的是淘天搜索向 **Agent 导购** 升级的方向，体现的是 AI 产品经理把大模型能力产品化的能力，而不是做一个普通聊天机器人。

---

## 1 分钟中文介绍

我在作品集里做了一个 **AI Shopping Guide Agent**，解决的是电商里「用户说不清需求、搜不准、不信推荐」这三个问题。

产品上，用户用中文直接输入，比如「留学生写代码的轻薄本，预算 6000 左右」或「手机预算 5000 到 7000」。系统先做 **Intent Parsing**，支持预算区间；再从 CSV 商品库做 **RAG 召回**；然后用 **7 个维度**——品类、预算、评分、销量、偏好、库存、利润——加权排序出 Top 3。

和 Chatbot 不同的是，我有结构化排序、分数拆解、负向解释，还有 **Prompt Design** 和 **Follow-up** 追问。数据侧设计了 6 个埋点和 AB 实验，Primary 看 CTR 和加购，Guardrail 看 GMV。

这个 Demo 本地能跑，不需要 API Key，适合展示我对淘天 **搜索、推荐、Agent、RAG、实验** 的产品理解。

---

## 3 分钟中文介绍

### 1. 背景与动机（约 40 秒）

淘天这类平台的搜索，很多 Query 其实不是单个关键词，而是一整段购买意图，比如场景、预算、偏好叠在一起。传统搜索让用户反复改词，决策成本高，也影响 CTR 和加购。

我认为大模型时代的搜索升级，不是加一个聊天框，而是做 **导购 Agent**：理解意图、召回商品、排序、解释、还能追问澄清，并且 **可度量、可实验**。

### 2. 方案概览（约 60 秒）

Demo 的链路是：

**Query → Intent → RAG Recall → Ranking → Explanation → Follow-up → Analytics**

- **Intent**：规则 + `budget_parser` 做预算范围优先识别  
- **RAG**：24 个 SKU 的 CSV 知识库，先品类和预算过滤  
- **Ranking**：7 维加权，偏好和预算权重较高，同时保留 profit_score 平衡 GMV  
- **Explanation**：每个 Top 商品有理由、分数拆解，未进 Top 的也有原因  
- **实验**：6 个埋点 + AB（A 关键词搜索 vs B Agent）

界面上自然语言是主入口，预算品类偏好是可编辑的辅助约束，支持 Manual Override，这比较贴近真实导购产品。

### 3. 差异化（约 40 秒）

三点和「普通 Chatbot」不同：

1. **输出约束在真实 SKU 上**，不是幻觉编造商品  
2. **排序可解释、可调参**，方便和算法、运营讨论  
3. **有埋点和 AB**，能谈怎么验证、怎么迭代  

### 4. 局限与迭代（约 20 秒）

当前是 Mock LLM + 规则 NLU，适合作品集。上线会接真实商品中台、向量检索、在线学习和风控。我清楚 Demo 和生产的差距，也知道下一步怎么拆。

### 5. 收尾（约 20 秒）

如果您愿意，我可以现场演示「预算 5000 到 7000」的解析，以及 Top 3 的 Score Breakdown。这个项目我希望表达的是：**我能把大模型能力设计成淘天导购场景里可落地、可实验的产品。**

---

## 面试官可能问的 10 个问题 · 参考回答

### 1. 这个项目为什么适合淘天？

淘天的核心是 **流量转化和 GMV**，搜索和导购是高频入口。这个项目直接对应「搜索升级 + 智能导购」：用 Agent 理解复杂 Query、缩短决策路径、提升 CTR 和加购，同时用埋点和 AB 保证可迭代。模块划分（意图、召回、排序、解释）也和淘天内部搜推、对话、广告的协作方式一致，不是脱离业务的纯技术 Demo。

---

### 2. 它为什么不是普通 Chatbot？

Chatbot 的目标是开放式对话；导购 Agent 的目标是 **在真实商品约束下完成购买决策**。

本项目的差异在于：

- 有 **商品知识库（RAG）**，不能编造 SKU  
- 有 **多目标排序**，不是只生成一段话  
- 有 **可解释输出**（分数拆解、负向解释）  
- 有 **Multi-turn 澄清**，但澄清是为了补全意图槽位，不是为了闲聊  
- 有 **埋点与 AB**，能谈业务效果验证  

---

### 3. RAG 在这里体现在哪里？

RAG 体现在 **用商品知识库增强推荐，而不是让模型凭空生成商品**。

Demo 里 `products.csv` 是 Knowledge Base，包含价格、评分、销量、标签、库存、利润分等字段。流程是：先按品类和预算 **Retrieve** 候选，再把检索结果和用户意图一起用于排序和 Prompt 展示。V2 可以把标签匹配升级为向量检索，但产品逻辑不变：**先召回、再排序、再生成解释**。

---

### 4. Agent 在这里体现在哪里？

Agent 体现在 **多步骤、有目标的任务编排**，而不是单轮问答：

1. Intent Agent：解析需求  
2. Retrieval：召回候选  
3. Ranking：打分排序  
4. Explanation：生成理由与负向解释  
5. Follow-up：追问澄清  
6. Analytics：实验与埋点  

`app.py` 里用户点击「生成推荐」会走完整 Pipeline。未来可以用 LangGraph 等把各步做成可观测、可插拔的子 Agent，但产品语义已经是 Agent，而不是 FAQ Bot。

---

### 5. 推荐排序怎么设计？

排序分两步：**过滤 + 打分**。

过滤：品类匹配、`price ≤ budget_max`（预算区间时区间内商品 budget_score 最高）。

打分 7 维：品类 15%、预算 20%、评分 15%、销量 10%、偏好 25%、库存 5%、利润 10%。偏好和预算权重高，是因为导购场景里「符合用户说的话」最重要；利润分用于 **体验和 GMV 的平衡**，权重可控。

若候选过少，有分级兜底，但优先保证不超预算上限，避免把明显超预算的商品推进 Top 3。

---

### 6. AB Test 怎么验证效果？

设计 A/B 两组：A 传统关键词搜索，B AI 导购 Agent，50/50 分流。

- **Primary**：CTR（`recommendation_click`）、加购率（`add_to_cart`）、转化率  
- **Secondary**：Query 改写率、满意度（`recommendation_feedback`）  
- **Guardrail**：GMV per session，防止只追点击却伤害成交  

Demo 里是 Mock 看板，展示指标口径和看板形态。真实上线需要样本量估计、显著性检验、分层（新客 / 老客、品类）。PM 的价值是定义 **看什么指标、什么算赢、什么算伤**。

---

### 7. 如果接入真实淘宝商品数据，你会怎么改？

分四层改：

1. **数据层**：接商品中台（SPU/SKU、价格、库存、营销标签、评价），替换 CSV，建立更新机制  
2. **召回层**：品类 + 价格 + 向量语义检索 + 业务规则（区域、发货、活动价）  
3. **排序层**：离线 LTR + 在线重排，融入用户画像、实时行为；AB 调权重  
4. **生成层**：LLM 只负责话术和解释，**商品 ID 必须来自检索结果**（Function Calling / JSON Schema）  

同时补全风控、合规、缓存和降级策略，避免模型延迟拖垮首屏。

---

### 8. 如何避免大模型幻觉？

产品侧 **四层防护**：

1. **RAG 约束**：只推荐检索到的 SKU，Prompt 里写死「不得编造未召回商品」  
2. **结构化输出**：推荐结果以表格和字段为主，LLM 补充说明  
3. **规则兜底**：预算、品类用规则解析，不完全依赖模型  
4. **风险标注**：超预算、库存低、场景不匹配时显式提示  

评测上可增加「商品 ID 命中率」「字段一致性」等离线指标，而不只看文案是否流畅。

---

### 9. 如何平衡用户体验和 GMV？

用 **多目标排序 + AB Guardrail**，而不是二选一。

- 体验侧：偏好匹配、预算匹配、评分权重高，保证「推得对」  
- GMV 侧：`profit_score` 占 10%，可在实验中调节；也可用 AB 比较「零利润权重」与「基准权重」  
- **Guardrail**：若 GMV per session 下降超过阈值，回滚或降利润权重  

PM 要定义策略开关和实验假设，例如大促期间提高转化相关权重，平时平衡 NPS 与 GMV。

---

### 10. 你作为 AI 产品经理的价值是什么？

不是替算法写模型，而是 **定义问题、设计链路、规定度量、推动迭代**：

- 把「大模型能聊天」翻译成「导购 Agent 该有哪些步骤、输出什么」  
- 设计意图槽位、预算区间、Manual Override 等 **可落地的交互**  
- 规定 CTR、加购、改写率、GMV 等指标与 AB 方案  
- 识别幻觉、偏差、延迟等风险并写 Guardrail  
- 协调算法、工程、运营在同一套漏斗语言下协作  

这个 Demo 证明我能把上述工作 **做成可演示、可讨论的产品原型**，而不只是写 PPT。

---

## 演示检查清单（面试前）

- [ ] `streamlit run app.py` 可正常打开  
- [ ] 预算区间例句解析正确（5000–7000）  
- [ ] Top 3 + Score Breakdown 可展示  
- [ ] 「Why not others」有内容  
- [ ] 侧边栏埋点、AB 模块可展开  
- [ ] 准备好 30 秒 / 1 分钟版本各练一遍  

---

*相关文档：[`README.md`](README.md) · [`portfolio_note.md`](portfolio_note.md)*
